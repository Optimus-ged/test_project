import 'package:flutter/material.dart';

class AddAaffectation extends StatelessWidget {
  const AddAaffectation({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}